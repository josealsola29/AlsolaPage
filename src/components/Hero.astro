<section class="hero">

  <div class="hero-content container">

    <p class="greeting">
      Hola, soy
    </p>

    <h1>
      José Alsola
    </h1>

    <h2>
      Desarrollador de
      <span>Software</span>
    </h2>

    <p class="description">
      Desarrollo soluciones de software con especial
      interés en aplicaciones móviles, Android y
      tecnologías modernas.
    </p>

    <div class="actions">

      <a
        class="button button-primary"
        href="#projects"
      >
        Ver proyectos
      </a>

      <a
        class="button button-secondary"
        href="#contact"
      >
        Contactarme
      </a>

    </div>

  </div>

</section>

<style>
  .hero {
    min-height:
      calc(100vh - 72px);

    display: flex;
    align-items: center;

    background:
      radial-gradient(
        circle at 75% 25%,
        #eff6ff 0,
        transparent 35%
      );
  }

  .hero-content {
    padding-block: 6rem;
  }

  .greeting {
    margin: 0 0 0.75rem;

    color: var(--color-primary);

    font-size: 1rem;
    font-weight: 700;

    letter-spacing: 0.04em;
  }

  h1 {
    margin: 0;

    max-width: 850px;

    font-size:
      clamp(3.5rem, 9vw, 6.5rem);

    line-height: 0.95;

    letter-spacing: -0.055em;
  }

  h2 {
    margin: 1.25rem 0 0;

    font-size:
      clamp(1.7rem, 4vw, 2.8rem);

    font-weight: 500;

    line-height: 1.15;

    letter-spacing: -0.03em;

    color: var(--color-text-muted);
  }

  h2 span {
    color: var(--color-primary);
  }

  .description {
    max-width: 650px;

    margin: 2rem 0 0;

    color: var(--color-text-muted);

    font-size: 1.15rem;
    line-height: 1.8;
  }

  .actions {
    margin-top: 2.25rem;

    display: flex;
    flex-wrap: wrap;

    gap: 0.9rem;
  }

  @media (max-width: 600px) {
    .hero {
      min-height: auto;
    }

    .hero-content {
      padding-block: 5rem;
    }

    h1 {
      font-size:
        clamp(3.2rem, 15vw, 4.5rem);
    }

    .actions {
      align-items: stretch;
      flex-direction: column;

      max-width: 320px;
    }
  }
</style>
